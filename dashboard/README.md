# Werld Observatory — Dashboard

Next.js dashboard for the Werld simulation. See the [root README](../README.md) for setup.

```bash
npm install
npm run dev
```

Runs on [http://localhost:3000](http://localhost:3000). Reads from `../data/simulation.db` — start the simulation first.
